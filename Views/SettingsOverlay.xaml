<UserControl x:Class="ProjectCatalyst.Views.SettingsOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Focusable="True"
             KeyDown="OnKeyDown">

	<Grid>
		<Border Background="#CC05070D" />

        <Border x:Name="Panel"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="0,0,1,0"
                HorizontalAlignment="Right"
                Width="520">
            <Border.RenderTransform>
                <TranslateTransform x:Name="PanelTransform" X="520" />
            </Border.RenderTransform>

            <Grid Margin="40,50,40,40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Settings" Style="{StaticResource TitleText}" FontSize="32" />
                <TextBlock Grid.Row="1" Text="Esc / B to go back" Style="{StaticResource SubtitleText}" Margin="0,4,0,30" />

                <StackPanel Grid.Row="2">

                    <TextBlock Text="ACCENT COLOR" Style="{StaticResource HintText}" Margin="0,0,0,10" />
                    <ItemsControl x:Name="AccentSwatches">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <TextBlock Text="DISPLAY" Style="{StaticResource HintText}" Margin="0,30,0,10" />
                    <CheckBox x:Name="FullscreenCheckBox"
                              Content="Launch in fullscreen"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              IsChecked="True"
                              Margin="0,0,0,10" />

                    <TextBlock Text="CONTROLLER" Style="{StaticResource HintText}" Margin="0,20,0,10" />
                    <TextBlock Text="Left stick deadzone" Style="{StaticResource SubtitleText}" Margin="0,0,0,6" />
                    <Slider x:Name="DeadzoneSlider" Minimum="0.1" Maximum="0.6" Value="0.35" />

                    <TextBlock Text="LAUNCHER PATHS" Style="{StaticResource HintText}" Margin="0,30,0,10" />
                    <TextBlock Style="{StaticResource SubtitleText}" TextWrapping="Wrap"
                               Text="Executable paths are configured per-platform in MainWindow.LoadPlatforms(). A future version can surface per-tile path editing here." />

                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Exit Project Catalyst" Style="{StaticResource CatalystButton}" Margin="0,0,12,0" Click="OnExitAppClick" />
                    <Button Content="Back" Style="{StaticResource CatalystButton}" Click="OnBackClick" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
