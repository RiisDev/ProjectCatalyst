<UserControl x:Class="ProjectCatalyst.Views.Consoles.Ps3.ScreenshotViewerOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Focusable="True"
             KeyDown="OnKeyDown">

    <Grid>
        <Border x:Name="Backdrop" Background="#EE05070D" Opacity="0" />

        <Grid x:Name="Card"
              Margin="60"
              Opacity="0"
              RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <ScaleTransform x:Name="CardScale" ScaleX="0.96" ScaleY="0.96" />
            </Grid.RenderTransform>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="20"
                    Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="10" BlurRadius="40" Opacity="0.5" />
                </Border.Effect>
                <Image x:Name="PhotoImage" Stretch="Uniform" />
            </Border>

            <StackPanel Grid.Row="1" Margin="0,20,0,0" HorizontalAlignment="Center">
                <TextBlock x:Name="FileNameText"
                           Style="{StaticResource TitleText}"
                           FontSize="20"
                           HorizontalAlignment="Center" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0">
                    <TextBlock x:Name="PositionText"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontFamily="Segoe UI"
                               FontSize="14"
                               Margin="0,0,24,0" />
                    <TextBlock Text="&#x2190; &#x2192; Browse    Esc / B  Close"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontFamily="Segoe UI"
                               FontSize="14" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
