<UserControl x:Class="ProjectCatalyst.Views.Consoles.Ps3.VolumeAdjustOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Focusable="True"
             KeyDown="OnKeyDown">

    <Grid>
        <Border x:Name="Backdrop" Background="#CC05070D" Opacity="0" />

        <Border x:Name="Card"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="20"
                Width="560"
                Padding="40,34"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Opacity="0"
                RenderTransformOrigin="0.5,0.5">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="10" BlurRadius="40" Opacity="0.5" />
            </Border.Effect>
            <Border.RenderTransform>
                <ScaleTransform x:Name="CardScale" ScaleX="0.94" ScaleY="0.94" />
            </Border.RenderTransform>

            <StackPanel>
                <TextBlock x:Name="TitleTextBlock" Text="Volume" Style="{StaticResource TitleText}" FontSize="26" Margin="0,0,0,24" />

                <Slider x:Name="VolumeSlider"
                        Style="{StaticResource CatalystSlider}"
                        Minimum="0" Maximum="1"
                        SmallChange="0.05" LargeChange="0.1"
                        ValueChanged="OnSliderChanged" />

                <TextBlock x:Name="PercentText"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0,14,0,24" />

                <TextBlock Text="&#x2190; &#x2192; Adjust    Enter / A  Confirm    Esc / B  Cancel"
                           Style="{StaticResource HintText}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
